websockets==12.0
torch==2.3.0 # Or compatible version for your GPU/CPU
torchaudio==2.3.0 # Must match torch version
TTS==0.22.0 # Coqui TTS library
numpy==1.26.4
scipy==1.13.0 # For potential WAV writing if needed
